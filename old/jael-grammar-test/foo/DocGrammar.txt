The Language Grammar
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of Grammar==

===Literals===









UIdent literals are recognized by the regular expression
`````upper (letter | digit | '_')*`````

LIdent literals are recognized by the regular expression
`````'_'* lower (letter | digit | '_')*`````

NegChan literals are recognized by the regular expression
`````'^' '_'* lower (letter | digit | '_')*`````

ReftIdent literals are recognized by the regular expression
`````''' lower (letter | digit | '_')*`````

DecInt literals are recognized by the regular expression
`````'0' | ["123456789"] digit*`````

HexInt literals are recognized by the regular expression
`````{"0x"} ["0123456789abcdefABCDEF"]+`````

OctInt literals are recognized by the regular expression
`````{"0o"} ["01234567"]+`````

BinInt literals are recognized by the regular expression
`````{"0b"} ["01"]+`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in Grammar are the following:
  | ``case`` | ``cases`` | ``coind`` | ``corec``
  | ``data`` | ``dual`` | ``else`` | ``forall``
  | ``if`` | ``in`` | ``let`` | ``new``
  | ``of`` | ``proc`` | ``select`` | ``then``
  | ``type`` |  |  |

The symbols used in Grammar are the following:
  | , | -> | : | {}
  | { | } | | | (
  | ) | [ | ; | ]
  | (| | |) | $ | @
  | _ | ~ | || | =
  | \( | \case | <-> | -->
  | && | == | != | >=
  | <= | > | < | +
  | - | * | / | %
  | ! | . | \ | <-
  | := | ? | & |

===Comments===
Single-line comments begin with //.There are no multiple-line comments in the grammar.

==The syntactic structure of Grammar==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Label// | -> | //LIdent//
  |  |  **|**  | //DecInt//
  | //AnyInt// | -> | //DecInt//
  |  |  **|**  | //HexInt//
  |  |  **|**  | //OctInt//
  |  |  **|**  | //BinInt//
  | //LSpc1LIdent// | -> | //LIdent// //LSpc1LIdent//
  |  |  **|**  | //LIdent//
  | //LCom1LIdent// | -> | //LIdent// ``,`` //LCom1LIdent//
  |  |  **|**  | //LIdent//
  | //BaseType0// | -> | //RefinedType11// ``->`` //RefinedType//
  |  |  **|**  | //LIdent// ``:`` //RefinedType11// ``->`` //RefinedType//
  | //BaseType1// | -> | //LIdent//
  |  |  **|**  | ``{}``
  |  |  **|**  | ``{`` //LIdent// ``}``
  |  |  **|**  | ``{`` //LCom1RowType// ``}``
  |  |  **|**  | ``{`` //LCom1RowType// ``|`` //LIdent// ``}``
  |  |  **|**  | ``(`` //LCom2RefType// ``)``
  |  |  **|**  | ``[`` //RefinedType// ``;`` //AnyInt// ``]``
  | //BaseType2// | -> | //Alias//
  | //BaseType10// | -> | //BaseType0//
  | //BaseType11// | -> | //BaseType1//
  |  |  **|**  | //BaseType2//
  | //BaseType// | -> | //BaseType10//
  |  |  **|**  | //BaseType11//
  | //BaseType99// | -> | //BaseType0//
  |  |  **|**  | //BaseType1//
  | //LCom1RowType// | -> | //LIdent// ``:`` //RefinedType// ``,`` //LCom1RowType//
  |  |  **|**  | //LIdent// ``:`` //RefinedType//
  | //LCom1RefType// | -> | //RefinedType// ``,`` //LCom1RefType//
  |  |  **|**  | //RefinedType//
  | //LCom2RefType// | -> | //RefinedType// ``,`` //LCom2RefType//
  |  |  **|**  | //RefinedType// ``,`` //RefinedType//
  | //RefinedType0// | -> | //BaseType0//
  | //RefinedType1// | -> | ``(|`` //LIdent// ``:`` ``(`` //BaseType10// ``)`` ``|`` //Expr// ``|)``
  |  |  **|**  | ``(|`` //LIdent// ``:`` //BaseType11// ``|`` //Expr// ``|)``
  |  |  **|**  | //BaseType1//
  | //RefinedType2// | -> | //BaseType2//
  | //RefinedType10// | -> | //RefinedType0//
  | //RefinedType11// | -> | //RefinedType1//
  |  |  **|**  | //RefinedType2//
  |  |  **|**  | ``(`` //RefinedType0// ``)``
  | //RefinedType// | -> | //RefinedType10//
  |  |  **|**  | //RefinedType11//
  | //RefinedType99// | -> | //RefinedType0//
  |  |  **|**  | //RefinedType1//
  | //Pattern1// | -> | //LBar2Pattern//
  |  |  **|**  | //Pattern2//
  | //Pattern2// | -> | ``$`` //LIdent//
  |  |  **|**  | ``$`` //LIdent// ``@`` //Pattern4//
  |  |  **|**  | //Pattern3//
  | //Pattern3// | -> | //LIdent// ``(`` //LCom1Pattern// ``)``
  |  |  **|**  | //LIdent//
  |  |  **|**  | //Pattern4//
  | //Pattern4// | -> | ``_``
  |  |  **|**  | //AnyInt//
  |  |  **|**  | ``~`` //DecInt//
  |  |  **|**  | ``{}``
  |  |  **|**  | ``{`` //LCom1RecPat// ``}``
  |  |  **|**  | ``{`` //LCom1RecPat// ``|`` ``$`` //LIdent// ``}``
  |  |  **|**  | ``{`` //LCom1RecPat// ``|`` ``_`` ``}``
  |  |  **|**  | ``(`` //LCom2Pattern// ``)``
  |  |  **|**  | ``[`` //LCom1Pattern// ``]``
  |  |  **|**  | ``(`` //Pattern// ``)``
  | //Pattern// | -> | //Pattern1//
  | //LCom1Pattern// | -> | //Pattern// ``,`` //LCom1Pattern//
  |  |  **|**  | //Pattern//
  | //LCom2Pattern// | -> | //Pattern// ``,`` //LCom2Pattern//
  |  |  **|**  | //Pattern// ``,`` //Pattern//
  | //LBar2Pattern// | -> | //Pattern2// ``||`` //LBar2Pattern//
  |  |  **|**  | //Pattern2// ``||`` //Pattern2//
  | //LCom1RecPat// | -> | //LIdent// ``=`` //Pattern// ``,`` //LCom1RecPat//
  |  |  **|**  | //LIdent// ``=`` //Pattern//
  | //Expr101// | -> | ``\(`` //LCom1Pattern// ``)`` ``->`` //Expr//
  | //Expr102// | -> | ``\case`` ``{`` //LSemi1Case// ``}``
  | //Expr1// | -> | //Expr101//
  |  |  **|**  | //Expr102//
  |  |  **|**  | //Expr2//
  | //LSemi1Case// | -> | //Pattern// ``->`` //Expr// ``;`` //LSemi1Case//
  |  |  **|**  | //Pattern// ``->`` //Expr//
  | //Expr201// | -> | ``case`` //Expr// ``of`` ``{`` //LSemi1Case// ``}``
  | //Expr202// | -> | ``let`` ``{`` //LSemi1Let// ``}`` ``in`` //Expr2//
  | //Expr2// | -> | //Expr201//
  |  |  **|**  | //Expr202//
  |  |  **|**  | //Expr3//
  | //LSemi1Let// | -> | //Pattern// ``=`` //Expr// ``;`` //LSemi1Let//
  |  |  **|**  | //Pattern// ``=`` //Expr//
  | //Expr301// | -> | ``if`` //Expr// ``then`` //Expr3// ``else`` //Expr3//
  | //Expr302// | -> | ``if`` ``{`` //LSemi1Multi// ``}``
  | //Expr3// | -> | //Expr301//
  |  |  **|**  | //Expr302//
  |  |  **|**  | //Expr4//
  | //LSemi1Multi// | -> | //Expr2// ``->`` //Expr// ``;`` //LSemi1Multi//
  |  |  **|**  | //Expr2// ``->`` //Expr//
  | //Expr4// | -> | //Expr5// ``<->`` //Expr4//
  |  |  **|**  | //Expr5//
  | //Expr5// | -> | //Expr6// ``-->`` //Expr6//
  |  |  **|**  | //Expr6//
  | //Expr6// | -> | //Expr7// ``||`` //Expr6//
  |  |  **|**  | //Expr7//
  | //Expr7// | -> | //Expr8// ``&&`` //Expr7//
  |  |  **|**  | //Expr8//
  | //Expr8// | -> | //Expr9// ``==`` //Expr9//
  |  |  **|**  | //Expr9// ``!=`` //Expr9//
  |  |  **|**  | //Expr9// ``>=`` //Expr9//
  |  |  **|**  | //Expr9// ``<=`` //Expr9//
  |  |  **|**  | //Expr9// ``>`` //Expr9//
  |  |  **|**  | //Expr9// ``<`` //Expr9//
  |  |  **|**  | //Expr9//
  | //Expr9// | -> | //Expr10// ``+`` //Expr9//
  |  |  **|**  | //Expr10// ``-`` //Expr9//
  |  |  **|**  | //Expr10//
  | //Expr10// | -> | //Expr11// ``*`` //Expr10//
  |  |  **|**  | //Expr11// ``/`` //Expr10//
  |  |  **|**  | //Expr11// ``%`` //Expr10//
  |  |  **|**  | //Expr11//
  | //Expr11// | -> | ``!`` //Expr11//
  |  |  **|**  | ``~`` //Expr11//
  |  |  **|**  | //Expr12//
  | //Expr1201// | -> | //Expr12// ``.`` //Label//
  |  |  **|**  | //Expr12// ``\`` //Label//
  | //Expr1202// | -> | //AnyInt//
  |  |  **|**  | //LIdent//
  |  |  **|**  | //ReftIdent//
  |  |  **|**  | //Expr12// ``(`` //LCom1Expr// ``)``
  |  |  **|**  | ``(`` //LCom2Expr// ``)``
  |  |  **|**  | ``[`` //LCom1Expr// ``]``
  | //Expr1203// | -> | ``{}``
  |  |  **|**  | ``{`` //LCom1RecExpr// ``}``
  |  |  **|**  | ``{`` //LCom1RecExpr// ``|`` //Expr// ``}``
  | //Expr12// | -> | //Expr1201//
  |  |  **|**  | //Expr1202//
  |  |  **|**  | //Expr1203//
  |  |  **|**  | ``(`` //Expr// ``)``
  | //LCom1Expr// | -> | //Expr// ``,`` //LCom1Expr//
  |  |  **|**  | //Expr//
  | //LCom2Expr// | -> | //Expr// ``,`` //LCom2Expr//
  |  |  **|**  | //Expr// ``,`` //Expr//
  | //LCom1RecExpr// | -> | //LIdent// ``=`` //Expr// ``,`` //LCom1RecExpr//
  |  |  **|**  | //LIdent// ``<-`` //LIdent// ``,`` //LCom1RecExpr//
  |  |  **|**  | //LIdent// ``:=`` //Expr// ``,`` //LCom1RecExpr//
  |  |  **|**  | //LIdent// ``=`` //Expr//
  |  |  **|**  | //LIdent// ``<-`` //LIdent//
  |  |  **|**  | //LIdent// ``:=`` //Expr//
  | //Expr// | -> | //Expr1//
  | //AnyType// | -> | //RefinedType99//
  |  |  **|**  | //Session99//
  |  |  **|**  | //Alias//
  | //LCom1AnyType// | -> | //AnyType// ``,`` //LCom1AnyType//
  |  |  **|**  | //AnyType//
  | //Alias// | -> | //UIdent//
  |  |  **|**  | //UIdent// ``(`` //LCom1AnyType// //MaybeLCom1Expr// ``)``
  | //MaybeLCom1Expr// | -> | ``;`` //LCom1Expr//
  |  |  **|**  | **eps**
  | //Session0// | -> | ``coind`` //UIdent// ``.`` //Session12//
  | //Session1// | -> | ``?`` ``[`` //AnyType// ``]`` //Session//
  |  |  **|**  | ``!`` ``[`` //AnyType// ``]`` //Session//
  |  |  **|**  | ``+`` ``[`` //LCom1SChoice// ``]``
  |  |  **|**  | ``&`` ``[`` //LCom1SChoice// ``]``
  |  |  **|**  | ``dual`` //Session2//
  |  |  **|**  | **eps**
  | //Session2// | -> | //Alias//
  | //Session12// | -> | //Session1//
  |  |  **|**  | //Session2//
  | //Session99// | -> | //Session0//
  |  |  **|**  | //Session1//
  | //Session// | -> | //Session0//
  |  |  **|**  | //Session1//
  |  |  **|**  | //Session2//
  | //LCom1SChoice// | -> | //LIdent// ``->`` //Session// ``,`` //LCom1SChoice//
  |  |  **|**  | //LIdent// ``->`` //Session//
  | //ChanExp// | -> | //NegChan//
  |  |  **|**  | //Expr//
  | //Chan// | -> | //LIdent//
  |  |  **|**  | //NegChan//
  | //Proc1// | -> | ``{`` //LBar2ParProc// ``}``
  | //Proc2// | -> | ``let`` //Pattern// ``=`` //Expr// //Proc//
  |  |  **|**  | ``new`` //LIdent// ``:`` //Session// //Proc//
  |  |  **|**  | //Chan// ``->`` //Pattern// //Proc//
  |  |  **|**  | //Chan// ``<-`` //ChanExp// //Proc//
  |  |  **|**  | //Chan// ``select`` //LIdent// //Proc//
  |  |  **|**  | //Chan// ``cases`` ``{`` //LCom1PChoice// ``}``
  |  |  **|**  | ``corec`` //LIdent// ``(`` //LCom1RecProc// ``)`` //Proc//
  |  |  **|**  | //LIdent// ``(`` //LCom1ChanExp// ``)``
  |  |  **|**  | //Chan// ``<->`` //Chan//
  |  |  **|**  | **eps**
  | //Proc// | -> | //Proc1//
  |  |  **|**  | //Proc2//
  | //LCom1ChanExp// | -> | //ChanExp// ``,`` //LCom1ChanExp//
  |  |  **|**  | //ChanExp//
  | //LCom1PChoice// | -> | //LIdent// ``->`` //Proc// ``,`` //LCom1PChoice//
  |  |  **|**  | //LIdent// ``->`` //Proc//
  | //LCom1RecProc// | -> | //Pattern// ``=`` //ChanExp// ``,`` //LCom1RecProc//
  |  |  **|**  | //Pattern// ``=`` //ChanExp//
  | //LBar2ParProc// | -> | //Proc2// ``|`` //LBar2ParProc//
  |  |  **|**  | //Proc2// ``|`` //Proc2//
  | //DataCon// | -> | //LIdent//
  |  |  **|**  | //LIdent// ``(`` //LCom1RefType// ``)``
  | //LBar1DataCon// | -> | //DataCon// ``|`` //LBar1DataCon//
  |  |  **|**  | //DataCon//
  | //TScheme// | -> | ``forall`` //LSpc1LIdent// ``.`` //RefinedType//
  |  |  **|**  | //RefinedType//
  | //Top// | -> | //LIdent// ``:`` //TScheme//
  |  |  **|**  | ``type`` //UIdent// ``=`` //AnyType//
  |  |  **|**  | ``type`` //UIdent// ``(`` //LCom1LIdent// ``)`` ``=`` //AnyType//
  |  |  **|**  | ``data`` //UIdent// ``=`` //LBar1DataCon//
  |  |  **|**  | ``data`` //UIdent// ``(`` //LCom1LIdent// ``)`` ``=`` //LBar1DataCon//
  |  |  **|**  | //LIdent// ``=`` //Expr//
  |  |  **|**  | //LIdent// ``(`` //LCom1Pattern// ``)`` ``=`` //Expr//
  |  |  **|**  | ``let`` //Pattern// ``=`` //Expr//
  |  |  **|**  | ``proc`` //LIdent// ``(`` //LCom1Pattern// ``)`` ``=`` //Proc//
  | //LTop// | -> | //Top// ``;`` //LTop//
  |  |  **|**  | //Top// ``;``


